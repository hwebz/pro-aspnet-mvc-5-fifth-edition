@model string[]
@{
    ViewBag.Title = "Index";
}

@section Header {
    <div class="view">
        @foreach(string str in new [] { "Home", "List", "Edit"})
        {
            @Html.ActionLink(str, str, null, new { style = "margin: 5px" })
        }
    </div>    
}

<h2>Index</h2>

@*@foreach(string name in Model)
{
    <span><b>@name</b></span>
}*@
@Html.Partial("MyStronglyTypedPartial", new[] { "Apple", "Orange", "Pear" })

@Html.Action("Time", "Home", new { time = DateTime.Now })

@section Footer {
    <div class="view">
        This is the footer
    </div>    
}

@section scripts {
    <script>
        alert("scripts from Index.cshtml view");
    </script>    
}